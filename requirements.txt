# =====================================================
# ArchiRAG 核心依赖（严格按项目方案技术栈）
# Python 3.11+
# =====================================================

# --- Web 层 ---
fastapi>=0.110
uvicorn[standard]>=0.27
httpx>=0.27            # FastAPI 测试 + 内部 HTTP 调用
requests>=2.31

# --- RAG / LangChain ---
langchain>=0.2.0
langchain-core>=0.2.0
langchain-community>=0.2.0
faiss-cpu>=1.8.0       # 向量库（本地索引，无外部服务）
rank-bm25>=0.2.2       # 真正的 BM25 倒排检索（混合检索用）
numpy>=1.26

# --- Embedding 调用（OpenAI 兼容协议，Qwen DashScope 同协议）---
openai>=1.30

# --- 存储栈（严格按方案：MySQL + Redis；缺失时自动降级）---
SQLAlchemy>=2.0
PyMySQL>=1.1           # MySQL 驱动
redis>=5.0

# --- 配置 / 工具 ---
pydantic>=2.6
pydantic-settings>=2.2
python-dotenv>=1.0
watchdog>=4.0          # 规范文件监控 + 增量同步

# --- 规范文档解析 ---
python-docx>=1.1       # .docx
pdfplumber>=0.11       # .pdf（含表格提取）

# --- 前端 ---
streamlit>=1.33

# --- 测试 ---
pytest>=8.0
